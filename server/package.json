{
  "name": "grindup-graphql-gateway",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "GrindUp 앱용 얇은 GraphQL 게이트웨이 (Firestore REST 패스스루)",
  "scripts": {
    "dev": "tsx watch --env-file-if-exists=.env src/index.ts",
    "start": "tsx --env-file-if-exists=.env src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@apollo/server": "^5.5.1",
    "graphql": "^16.14.2"
  },
  "devDependencies": {
    "@types/node": "^22.18.0",
    "tsx": "^4.23.1",
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=22.9"
  }
}
